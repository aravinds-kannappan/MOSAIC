\documentclass[10pt,twocolumn]{article}

\usepackage[margin=0.85in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\usepackage[ruled,vlined]{algorithm2e}
\usepackage[colorlinks=true,allcolors=blue!55!black]{hyperref}
\usepackage[numbers,sort&compress]{natbib}

% , - arXiv-style headings (small caps) , , , , , , , , , , , , -
\titleformat{\section}{\large\bfseries\scshape}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries\scshape}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}{\normalsize\itshape}{\thesubsubsection}{0.6em}{}
% Run-in paragraph leads are italic, not bold, so the sub-topics read as part of
% the prose rather than as disconnected bold labels.
\titleformat{\paragraph}[runin]{\normalfont\itshape}{}{0pt}{}\titlespacing*{\paragraph}{0pt}{4pt}{0.5em}
\captionsetup{font=small,labelfont=bf}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.05}

\theoremstyle{definition}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{assumption}{Assumption}

\newcommand{\Rt}{R_t}
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Prob}{\mathbb{P}}
\newcommand{\Var}{\operatorname{Var}}
\newcommand{\KL}{D_{\mathrm{KL}}}
\newcommand{\Pois}{\mathrm{Poisson}}
\newcommand{\Gam}{\mathrm{Gamma}}
\newcommand{\NB}{\mathrm{NegBin}}
\newcommand{\Dir}{\mathrm{Dir}}
\newcommand{\jsd}{\mathrm{JSD}}
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\github}{\href{https://github.com/aravinds-kannappan/MOSAIC}{Github}}

\begin{document}

% , - Title block (spans both columns) , , , , , , , , , , , , --
\twocolumn[{%
\begin{center}
\vspace{2pt}
{\LARGE\scshape MOSAIC: Multi-Modal Open Surveillance\\[2pt]
with AI-Driven Calibrated Inference\par}
\vspace{8pt}
{\small\scshape Preprint, compiled \today\par}
\vspace{16pt}
{\large Aravind Kannappan\par}
\vspace{14pt}
\end{center}

\begin{center}{\scshape\bfseries Abstract}\end{center}
\begin{center}\begin{minipage}{0.86\textwidth}\small\noindent
\textbf{Background and objective.} Pandemic early warning fails less often from a
lack of data than from a lack of integration and calibration. Wastewater viral
load, pathogen genomes, and outbreak text reports arrive on different cadences,
in different units, and with different noise processes, and the probabilities
ultimately shown to decision-makers are almost never validated. My objective is
to fuse these three public, authentication-free streams into a single calibrated
and falsifiable quantity, the posterior probability that the instantaneous
reproduction number exceeds one, $\Prob(\Rt>1)$, which is the probability that
transmission is currently growing.
\\[3pt]
\textbf{Methods.} I specify a renewal-equation latent-incidence model with
stream-specific observation kernels (negative-binomial for wastewater, Poisson
for text-event counts, Dirichlet--multinomial for lineage frequencies) and derive
each detector from first principles: a Poisson--Gamma Bayesian online
change-point detector with a windowed-maximum alarm, a Jensen--Shannon
genomic-anomaly detector with a rolling null, an EpiEstim reproduction-number
estimator, and a fusion rule that reduces, in the full backend, to a hierarchical
posterior sampled by the No-U-Turn Sampler. I validate $\Prob(\Rt>1)$ as a
prospective probabilistic forecast of realised growth on the multi-year CDC
National Wastewater Surveillance System record.
\\[3pt]
\textbf{Results.} Over $N{=}1{,}334$ day-ahead forecasts the system attains an
Expected Calibration Error of $0.086$, a Brier score of $0.124$, and an AUROC of
$0.917$; a Murphy decomposition attributes the Brier score to a small reliability
term ($0.010$) and a large resolution term ($0.136$), so the forecasts are both
well calibrated and discriminative, and the growth probability leads national wave
peaks by a median of $68$ days. I also surface and correct a numerical defect in
the reproduction-number tail probability that silently corrupts large-count
series.
\\[3pt]
\textbf{Conclusion.} An explicit, falsifiable target quantity together with a
faithful backend-free deployment is a practical route to trustworthy open early
warning.
\\[6pt]
\textbf{Github:}~\github
\end{minipage}\end{center}
\vspace{8pt}

\begin{center}\begin{minipage}{0.86\textwidth}\small
\textbf{Keywords}\quad epidemic early warning $\cdot$ wastewater surveillance
$\cdot$ effective reproduction number $\cdot$ Bayesian change-point detection
$\cdot$ probabilistic calibration $\cdot$ multi-modal data fusion
\end{minipage}\end{center}
\vspace{18pt}
}]

% ============================================================================
\section{Background}
\label{sec:intro}

Every major epidemic of the past two decades, the 2009 H1N1 pandemic, the
2013--2016 West African Ebola epidemic, the emergence of SARS-CoV-2 and its
successive variants, the 2022 multi-country mpox outbreak, and the ongoing
panzootic of highly pathogenic avian influenza, was, in retrospect, preceded by
signals that were detectable days to weeks before the official declaration. The
recurring post-mortem finding is not that the relevant data did not exist, but
that it was scattered across incompatible systems, expressed in incomparable
units, and never assembled into a single quantity that a decision-maker could
trust and act on.

The stakes of this gap are asymmetric. In the exponential-growth phase of an
epidemic, a detection delay of a single generation interval can multiply the
eventual burden, because the number of infections at the moment of intervention
sets the scale of everything that follows. Conversely, a \emph{false} alarm is
comparatively cheap if it is accompanied by an honest probability and an explicit
uncertainty, because a calibrated $30\%$ invites monitoring rather than
lockdown. This asymmetry is the reason I privilege early, probabilistic,
\emph{calibrated} signals over late, categorical, point alerts.

This points to two binding constraints, neither of which is data scarcity.
The first is \emph{integration}: municipal wastewater is sampled routinely and
published as a standing national program \citep{nwss,naughton2023}; pathogen
genomes are deposited continuously and shared openly with phylogenetic context
\citep{hadfield2018}; and structured outbreak reports are issued by the World
Health Organization and by moderated expert networks
\citep{promed,carlson2023}. Each is informative on its own, but they are
asynchronous (weekly versus continuous versus sporadic), differently scaled
(concentration percentiles versus categorical lineage frequencies versus event
counts), and differently noised (over-dispersed counts versus
Dirichlet--multinomial composition versus heavy-tailed reporting). Turning them
into one coherent signal is a statistical, not merely an engineering, problem.

The second constraint is \emph{calibration}. A system that emits a number, ``a
$78\%$ chance of growth'', is only useful if that number means what it says: if,
across the many times the system says $78\%$, the event happens about $78\%$ of
the time. Modern statistical and machine-learning systems are frequently and
badly miscalibrated \citep{guo2017}, and epidemic-forecasting tools rarely report
calibration at all, so a stated probability is too often an uninterpretable
score rather than an actionable frequency.

\paragraph{A falsifiable target.}
I argue that the right quantity for an early-warning system to emit is the
posterior probability that transmission is currently growing,
\begin{equation}
p_t \;=\; \Prob\!\big(\Rt > 1 \,\big|\, \text{data up to } t\big),
\label{eq:target}
\end{equation}
where $\Rt$ is the instantaneous (effective) reproduction number, the expected
number of secondary infections per infectious individual at time $t$. I choose
Eq.~\eqref{eq:target} for three reasons. It is \emph{unitless}, hence comparable
across pathogens, locations, and data modalities. It has a crisp
\emph{operational} meaning: $\Rt>1$ is exactly the condition under which the
epidemic curve is turning upward. And, crucially, it is \emph{falsifiable}: the
event $\{\Rt>1\}$ is, with a lag, observable through subsequent growth, so the
calibration of $p_t$ is something one can \emph{measure} rather than merely
assert. This last property is what allows me to report, in
\S\ref{sec:results}, an Expected Calibration Error for the live system rather
than a methodological promise.

\paragraph{Contributions.}
\begin{enumerate}[leftmargin=1.4em,itemsep=2pt,topsep=2pt]
\item \textbf{A complete probabilistic specification} (\S\ref{sec:model}--\ref{sec:fusion})
for multi-modal outbreak inference: a renewal latent-incidence process with
negative-binomial, Poisson, and Dirichlet--multinomial observation kernels, with
each stream detector, Poisson--Gamma BOCPD, Jensen--Shannon anomaly, EpiEstim
$\Rt$, derived from first principles, and a fusion rule that reduces to a
hierarchical posterior in the full backend.
\item \textbf{A two-tier open implementation} (\S\ref{sec:impl}): a serverless
TypeScript deployment that runs the entire pipeline with no backend, plus a
Python backend that performs the full No-U-Turn-Sampler posterior, designed so
the former is mathematically faithful to the latter.
\item \textbf{An empirical calibration study} (\S\ref{sec:results}) on the real
multi-year national wastewater record showing that the served $\Prob(\Rt>1)$ is
well-calibrated (ECE $0.086$) and discriminative (AUROC $0.917$), with a Murphy
decomposition of the Brier score, live multi-stream alert attribution, and a
documented numerical correction I recommend other implementations check.
\end{enumerate}

\paragraph{Why a backend-free deployment matters.}
A recurring failure of research surveillance tools is that they are demonstrated
once and then decay, because they depend on a server, a credentialed data feed,
or a maintainer's laptop. A second theme of this paper is therefore
\emph{operational}: the entire MOSAIC pipeline runs in a serverless edge
function with no backend and no authenticated data access, so the public
dashboard is always live and reproducible by anyone who forks the repository.
This constraint is not free, it forces every detector to have a closed-form or
streaming implementation and every data movement to be small, but I show it is
achievable without sacrificing the probabilistic target, and that meeting it
exposed real bugs (a quadratic genomic detector, a numerically unstable tail
probability, a self-referential fetch pattern) that a heavier prototype would
have hidden.

\paragraph{Organization.}
\S\ref{sec:related} surveys related work. \S\ref{sec:problem} fixes notation and
states the inference problem and its assumptions. \S\ref{sec:model} specifies the
generative model. \S\ref{sec:detectors} derives the three stream detectors and
\S\ref{sec:rt} the reproduction-number estimator, including the numerical
correction. \S\ref{sec:fusion} treats fusion and \S\ref{sec:forecast}
forecasting. \S\ref{sec:calmethod} defines the calibration instruments,
\S\ref{sec:impl} the implementation, and \S\ref{sec:results} the experiments.
\S\ref{sec:discuss}--\ref{sec:ethics} discuss limitations and dual-use, and
\S\ref{sec:conclusion} concludes.

% ============================================================================
\section{Related Literature}
\label{sec:related}

\paragraph{Wastewater-based epidemiology.}
Wastewater concentration of viral RNA tracks community prevalence and typically
leads clinical case reports, because shedding begins early in infection and is
independent of care-seeking and testing behaviour
\citep{nwss,naughton2023}. National programs now publish standardized activity
metrics; the U.S.\ NWSS reports, per site, a percentile of current activity
relative to that site's own history. Most public-facing tools display
concentrations or qualitative trend arrows. MOSAIC instead converts the national
record into a \emph{calibrated growth probability}, which is what makes its
output validatable.

\paragraph{Reproduction-number estimation.}
The instantaneous reproduction number is most commonly estimated by the
sliding-window Poisson--Gamma method of \citet{cori2013}, building on
renewal-process theory \citep{wallinga2004,fraser2007}. The method is attractive
because it is conjugate and closed-form, and because $\Prob(\Rt>1)$ falls out of
the Gamma posterior directly. Alternatives include Wallinga--Teunis
retrospective estimators and state-space / particle-filter formulations
\citep{flaxman2020}; I adopt EpiEstim as the renewal core precisely because its
closed form admits a faithful serverless implementation.

\paragraph{Change-point and anomaly detection.}
Bayesian Online Change-Point Detection \citep{adams2007} maintains a recursive
posterior over the time since the last change-point (the ``run length'') under
conjugate emissions, and is well suited to streaming count data. Distributional
shift in categorical lineage frequencies is naturally measured by the
Jensen--Shannon divergence \citep{lin1991}, a bounded, symmetric smoothing of the
Kullback--Leibler divergence whose square root is a metric \citep{endres2003}.
Sequence-level anomaly detection for novel pathogens is an active area
\citep{metagene2024}; MOSAIC operates at the coarser lineage-frequency level,
which is robust and available in near-real-time.

\paragraph{Multi-modal fusion in epidemiology.}
Nowcasting and forecasting hubs have demonstrated that ensembling and
multi-signal models outperform any single stream \citep{cramer2022}, and digital
/ event-based surveillance has a long history of mining text for outbreak
signals \citep{brownstein2009,carlson2023}. What is comparatively rare is a
single open system that fuses wastewater, genomic, and text evidence into one
probabilistic target and then reports that target's calibration; that gap is the
motivation for this work.

\paragraph{Probabilistic forecast calibration.}
The reliability diagram, the Brier score and its reliability--resolution--uncertainty
decomposition \citep{brier1950,murphy1973}, proper scoring rules
\citep{gneiting2007}, and the Expected Calibration Error \citep{guo2017} are the
standard instruments for asking whether stated probabilities match observed
frequencies. I use all of them.

\paragraph{Collaborative forecasting and pooling.}
Large-scale forecasting efforts have repeatedly found that combining models, by
ensembling, stacking, or opinion pooling, is more robust than any single
component, and that well-constructed ensembles are among the best-calibrated
forecasters available \citep{cramer2022}. Logarithmic and linear opinion pools
have a long statistical pedigree as ways to aggregate probabilistic judgements;
my lightweight fusion (\S\ref{sec:fusion}) is a weighted logarithmic pool of
per-stream alarms, specialised so that absent modalities neither vote nor dilute.

\paragraph{Probabilistic programming.}
The full hierarchical posterior is sampled with the No-U-Turn Sampler
\citep{hoffman2014} as implemented in NumPyro \citep{phan2019,bingham2019}, which
provides effect-handler-based composition and JIT-compiled, vectorized MCMC.
Conjugacy in the lightweight detectors (Poisson--Gamma for counts,
Dirichlet--multinomial for composition) is what allows the same models to be
evaluated in closed form on the edge without a sampler at all.

% ============================================================================
\section{Methods}

\subsection{Problem setup and notation}
\label{sec:problem}

I index discrete time (days) by $t$. The unit of analysis is a
\emph{surveillance cell} $(k,\ell)$: a pathogen $k$ in a location $\ell$. For a
fixed cell, let $I_t \ge 0$ denote latent new infections on day $t$. The three
streams provide noisy, lagged, partial observations of $I_{1:T}$:
\begin{itemize}[leftmargin=1.2em,itemsep=1pt,topsep=2pt]
\item wastewater activity $C_t$ (a non-negative, over-dispersed level);
\item text-event counts $E_t$ (non-negative integers, the number of WHO/ProMED
reports);
\item lineage frequency vectors $\mathbf L_t \in \Delta^{K-1}$ (a point on the
$(K{-}1)$-simplex over $K$ lineages).
\end{itemize}
The objects of inference are the latent incidence $I_{1:T}$ and the
time-varying reproduction number $\Rt$, and the reported quantity is the tail
posterior $p_t$ of Eq.~\eqref{eq:target}. Table~\ref{tab:notation} collects the
notation.

I make the following modelling assumptions.
\begin{assumption}[Renewal dynamics]\label{ass:renewal}
Incidence follows the renewal equation with a fixed, known generation-interval
distribution $w_{1:S}$ and a smoothly varying $\Rt$.
\end{assumption}
\begin{assumption}[Conditional independence]\label{ass:ci}
Given the latent path $I_{1:T}$ and stream-specific nuisance parameters, the
three streams are conditionally independent observations.
\end{assumption}
\begin{assumption}[Stationary observation nuisances]\label{ass:nuis}
Within an inference window the shedding scale, reporting rate, ascertainment, and
sequencing depth vary slowly relative to $\Rt$.
\end{assumption}
These assumptions are standard for renewal-based nowcasting; I revisit their
limits in \S\ref{sec:discuss}.

\begin{table}[t]
\centering\footnotesize
\caption{Notation.}
\label{tab:notation}
\begin{tabular}{ll}
\toprule
Symbol & Meaning \\
\midrule
$I_t$ & latent new infections on day $t$ \\
$\Rt$ & instantaneous reproduction number \\
$w_{1:S}$ & discretised generation-interval pmf \\
$\Lambda_t$ & total infectiousness $\sum_s w_s I_{t-s}$ \\
$C_t, E_t, \mathbf L_t$ & wastewater / text / genomic observations \\
$r_t$ & BOCPD run length (time since change) \\
$H$ & BOCPD hazard $1/\mu_{\mathrm{RL}}$ \\
$a^{(j)}_t$ & alarm of stream $j\in\{\text{txt,ww,gen}\}$ \\
$p_t$ & $\Prob(\Rt>1\mid\text{data}\le t)$ \\
$\Phi$ & standard normal CDF \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\subsection{The generative model}
\label{sec:model}

\subsubsection{Latent incidence and the renewal equation}
Under Assumption~\ref{ass:renewal}, expected incidence is the product of the
reproduction number and the total infectiousness contributed by past infections
\citep{wallinga2004,cori2013}:
\begin{equation}
\E[I_t \mid \Rt, I_{<t}] \;=\; \Rt \, \Lambda_t,
\qquad \Lambda_t = \sum_{s=1}^{S} w_s\, I_{t-s},
\label{eq:renewal}
\end{equation}
with $w_s\ge 0$, $\sum_s w_s = 1$. The reproduction number evolves as a
geometric random walk, which encodes the prior that transmissibility changes
smoothly rather than abruptly:
\begin{equation}
\log \Rt \;\sim\; \mathcal N\!\big(\log R_{t-1},\, \sigma_R^2\big).
\label{eq:rw}
\end{equation}
The generation interval is discretised from a Gamma density by midpoint
quadrature, $w_s \propto f_{\Gam}(s;\kappa,\theta)$ for $s=1,\dots,S$, and
renormalised; for SARS-CoV-2 I use mean $5.1$ and standard deviation $4.0$ days
\citep{he2020}, truncated at $S=21$.

The renewal equation is the discrete-time analogue of the Lotka integral equation
of demography and underlies essentially all modern $\Rt$ estimators
\citep{wallinga2004,fraser2007,cori2013}. Its key property for my purposes is
that $\Rt$ is a \emph{local} quantity: it depends only on the ratio of current
incidence to recent infectiousness, not on the absolute scale of the epidemic.
This is what lets me read $\{\Rt>1\}$ off a relative activity index such as the
wastewater percentile without knowing the true number of infections, and it is
why the threshold $\Rt=1$, rather than any particular incidence level, is the
natural alarm boundary: it is invariant to the unknown ascertainment and shedding
constants that scale the observations. In the small-$\sigma_R$ regime, the
exponential growth rate is approximately $\log\Rt$ divided by the mean generation
interval, so a sustained $\Rt$ a few percent above one corresponds to a doubling
time of weeks, fast enough to matter, slow enough to act on.

\subsubsection{Stream observation kernels}
The three streams are conditionally independent observations of the same latent
$I_{1:T}$ (Assumption~\ref{ass:ci}).

\paragraph{Wastewater (negative-binomial).}
Viral load is a lagged, over-dispersed, count-like measurement of prevalence:
\begin{equation}
C_t \;\sim\; \NB\!\big(\rho_W\, I_{t-d_W},\ \phi_W\big),
\label{eq:ww-obs}
\end{equation}
with shedding scale $\rho_W>0$, reporting delay $d_W$, and dispersion $\phi_W$.
The negative-binomial captures the extra-Poisson variance induced by
heterogeneous shedding, sampling, and assay noise.

\paragraph{Text (Poisson).}
The number of qualifying outbreak reports is modelled as Poisson in prevalence,
modulated by a slowly varying ascertainment $\hat q_t$:
\begin{equation}
E_t \;\sim\; \Pois\!\big(\lambda_N\, \hat q_t\, I_{t-d_N}\big),
\label{eq:text-obs}
\end{equation}
with reporting rate $\lambda_N$ and delay $d_N$.

\paragraph{Genomic (Dirichlet--multinomial).}
The lineage composition of sampled sequences is Dirichlet--multinomial, with the
composition map $\mathbf f$ linking incidence-weighted lineage prevalences to the
expected frequencies, sequencing depth $N_t$, and concentration $\kappa$
controlling over-dispersion relative to the multinomial:
\begin{equation}
\mathbf L_t \;\sim\; \mathrm{DirMult}\!\big(N_t,\ \kappa\, \mathbf f(I_{t-d_G},\boldsymbol\theta_L)\big).
\label{eq:gen-obs}
\end{equation}

\subsubsection{The joint posterior}
Collecting all parameters into $\Theta=(\Rt, I_{1:T}, \rho_W,\phi_W,\lambda_N,\kappa,\dots)$
and applying Assumption~\ref{ass:ci}, the joint posterior factorises over streams:
\begin{equation}
p(\Theta \mid \mathbf C,\mathbf E,\mathbf L)
\;\propto\;
p(\mathbf C\mid\Theta)\,p(\mathbf E\mid\Theta)\,p(\mathbf L\mid\Theta)\,p(\Theta).
\label{eq:joint}
\end{equation}
The full backend samples Eq.~\eqref{eq:joint} directly. Because that sampler is
unavailable in a backend-free deployment, the lightweight tier replaces the joint
fit with per-stream conjugate detectors (\S\ref{sec:detectors}) combined by an
independent-evidence rule (\S\ref{sec:fusion}); the two tiers share the
observation models \eqref{eq:ww-obs}--\eqref{eq:gen-obs} and the target
\eqref{eq:target}.

\paragraph{Priors and hyperparameters.}
The priors are weakly informative and chosen to be conservative
(Table~\ref{tab:priors}). The reproduction-number random walk uses a small
innovation scale $\sigma_R$ so that $\Rt$ is encouraged to change smoothly; the
EpiEstim window prior on $\Rt$ is a high-variance Gamma centred above one, which
is deliberately diffuse so that the data dominate; the BOCPD hazard corresponds
to an expected run length of a month, a neutral prior on the frequency of regime
change; and the genomic baseline spans ninety windows so that a transient
fluctuation is not mistaken for a sustained shift. None of these are tuned to the
evaluation; they are fixed a priori and reused across all results.

\begin{table}[t]
\centering\footnotesize
\caption{Default priors and hyperparameters (shared by both tiers).}
\label{tab:priors}
\begin{tabular}{@{}lll@{}}
\toprule
Quantity & Setting & Role \\
\midrule
$\log\Rt$ walk $\sigma_R$ & $0.1$ & $\Rt$ smoothness \\
EpiEstim $\Rt$ prior & $\Gam$, mean/sd $5$ & diffuse \\
EpiEstim window $\tau$ & $7$ d & estimation \\
Gen.\ interval & mean $5.1$, sd $4.0$ d & SARS-CoV-2 \\
BOCPD hazard $H$ & $1/30$ & run length \\
BOCPD prior $(\alpha_0,\beta_0)$ & $(1,1)$ & rate prior \\
JSD baseline $B$ & $90$ windows & rolling null \\
Forecast damping $\phi$ & $0.8$ & mean-reversion \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\subsection{Stream detectors}
\label{sec:detectors}

\subsubsection{Bayesian online change-point detection}
\label{sec:bocpd}
For the count streams I detect abrupt regime changes with BOCPD
\citep{adams2007}. Let $r_t\in\{0,1,2,\dots\}$ be the \emph{run length}, the
number of steps since the last change-point. BOCPD maintains the joint
$p(r_t, n_{1:t})$ and hence the run-length posterior $p(r_t\mid n_{1:t})$ by a
forward recursion with two messages: a \emph{growth} message (no change, run
length increments) and a \emph{change-point} message (run length resets to $0$).
With a constant hazard $H=1/\mu_{\mathrm{RL}}$,
\begin{align}
p(r_t{=}r{+}1, n_{1:t}) &= p(r_{t-1}{=}r, n_{1:t-1})\,(1{-}H)\,\pi_r(n_t),\label{eq:growth}\\
p(r_t{=}0, n_{1:t}) &= \sum_{r} p(r_{t-1}{=}r, n_{1:t-1})\,H\,\pi_r(n_t),\label{eq:reset}
\end{align}
where $\pi_r(n_t)=p(n_t\mid r_{t-1}{=}r, n_{1:t-1})$ is the posterior predictive
of the emission model conditioned on the data assigned to the current run.

\paragraph{Conjugate Poisson--Gamma predictive.}
I model counts as $n_t\mid\lambda\sim\Pois(\lambda)$ with a conjugate prior
$\lambda\sim\Gam(\alpha_0,\beta_0)$. Conditioned on a run that has accumulated
sufficient statistics $(\alpha,\beta)$, the predictive is obtained by
marginalising $\lambda$:
\begin{align}
\pi(n\mid\alpha,\beta)
&= \int_0^\infty \Pois(n\mid\lambda)\,\Gam(\lambda\mid\alpha,\beta)\,d\lambda \nonumber\\
&= \frac{\Gamma(\alpha+n)}{\Gamma(\alpha)\,n!}\Big(\tfrac{\beta}{\beta+1}\Big)^{\!\alpha}\Big(\tfrac{1}{\beta+1}\Big)^{\!n},
\label{eq:nb-pred}
\end{align}
which is exactly the negative-binomial $\NB(n;\alpha,\frac{\beta}{\beta+1})$. The
sufficient statistics update along each run by conjugacy as
$\alpha\!\leftarrow\!\alpha+n$, $\beta\!\leftarrow\!\beta+1$, with a fresh
$(\alpha_0,\beta_0)$ seeding every newly created run length.

\paragraph{The alarm: windowed maximum, not cumulative product.}
The instantaneous change-point probability is $P(r_t{=}0\mid n_{1:t})$ from the
normalised recursion. The stream alarm I report is its maximum over a recent
window of length $W$,
\begin{equation}
a^{\mathrm{cp}}_t \;=\; \max_{t-W < \tau \le t} P(r_\tau{=}0 \mid n_{1:\tau}).
\label{eq:cp-alarm}
\end{equation}
The windowed maximum is a deliberate design choice, justified by the following.
\begin{proposition}[Cumulative combination is degenerate under the null]
\label{prop:cum}
Under the null of no change-point, the per-step reset probability has a positive
floor $\approx H$. Hence the cumulative ``has a change occurred in the window''
probability $1-\prod_{\tau}(1-P(r_\tau{=}0))$ converges to $1$ as $W$ grows,
independently of the data.
\end{proposition}
\noindent\emph{Sketch.} If $P(r_\tau{=}0)\ge H>0$ for all $\tau$, then
$\prod_{\tau=t-W+1}^{t}(1-P(r_\tau{=}0)) \le (1-H)^{W}\to 0$, so the complement
$\to 1$. The windowed maximum \eqref{eq:cp-alarm} instead returns the single
strongest recent reset, which stays near the floor under the null and spikes only
on a genuine regime shift. $\square$

\paragraph{Cost.} The exact recursion is $O(t)$ per step because the run-length
support grows; truncating the support at a maximum run length (or pruning
low-mass hypotheses) makes it $O(1)$ amortized, which is what the implementation
uses. Algorithm~\ref{alg:bocpd} states the full procedure.

\begin{algorithm}[t]
\small
\caption{Poisson--Gamma BOCPD with windowed alarm}
\label{alg:bocpd}
\KwIn{counts $n_{1:T}$; prior $(\alpha_0,\beta_0)$; hazard $H$; window $W$}
\KwOut{change-point probabilities $g_{1:T}$; alarm $a^{\mathrm{cp}}_T$}
$R\leftarrow[1]$;\ \ $\boldsymbol\alpha\leftarrow[\alpha_0]$;\ \ $\boldsymbol\beta\leftarrow[\beta_0]$\;
\For{$t=1$ \KwTo $T$}{
  $\pi_r\leftarrow \NB\!\big(n_t;\alpha_r,\tfrac{\beta_r}{\beta_r+1}\big)$ for each run $r$\;
  $\text{grow}_r\leftarrow R_r(1-H)\pi_r$;\quad $\text{cp}\leftarrow\sum_r R_r H\pi_r$\;
  $R\leftarrow \text{normalize}\big([\text{cp},\ \text{grow}_0,\text{grow}_1,\dots]\big)$\;
  $\boldsymbol\alpha\leftarrow[\alpha_0,\ \boldsymbol\alpha+n_t]$;\quad $\boldsymbol\beta\leftarrow[\beta_0,\ \boldsymbol\beta+1]$\;
  $g_t\leftarrow R_0$ \tcp*{$P(r_t{=}0\mid n_{1:t})$}
}
$a^{\mathrm{cp}}_T\leftarrow \max_{T-W<\tau\le T} g_\tau$\;
\Return $g_{1:T},\ a^{\mathrm{cp}}_T$
\end{algorithm}

\subsubsection{Wastewater pipeline}
\label{sec:ww}
\paragraph{National aggregation.}
NWSS reports, per site $i$ and day $t$, a percentile $c_{i,t}\in[0,100]$ of
current $15$-day-averaged activity relative to that site's own history. I form a
national signal by simple aggregation over the reporting set $\mathcal S_t$,
\begin{equation}
\bar c_t \;=\; \frac{1}{|\mathcal S_t|}\sum_{i\in\mathcal S_t} c_{i,t},
\qquad |\mathcal S_t|\approx 1{,}200,
\label{eq:natagg}
\end{equation}
performed server-side via a single aggregation query so that the transferred
payload is a small daily series rather than millions of site-day rows. Because
$c_{i,t}$ is already a site-relative level, $\bar c_t$ is a dimensionless index of
national activity that is comparable across time despite a changing site panel.

\paragraph{Change-point and sustained elevation.}
I run the Poisson--Gamma BOCPD of \S\ref{sec:bocpd} on the discretised series
$n_t=\lfloor\bar c_t\rceil$ to obtain $a^{\mathrm{ww,cp}}_t$. Abrupt onset is only
half the signal: a slowly-rising but persistently elevated wave is also
informative and may not trip a change-point. Because the percentile is itself a
level, I add an elevation alarm and combine the two by a noisy-or,
\begin{align}
a^{\mathrm{ww,lvl}}_t &= \mathrm{clip}\!\Big(\tfrac{\bar c_t-50}{40},0,1\Big), \nonumber\\
a^{\mathrm{ww}}_t &= 1-(1-a^{\mathrm{ww,cp}}_t)(1-a^{\mathrm{ww,lvl}}_t).
\label{eq:ww-alarm}
\end{align}

\subsubsection{Text pipeline}
\label{sec:text}
\paragraph{Extraction.}
WHO Disease Outbreak News (queried newest-first) and ProMED posts are parsed into
structured events $(\text{pathogen},\text{location},\text{date},\text{counts},
\text{novelty})$. The full backend uses a constrained large-language-model
extractor; the lightweight tier uses curated patterns plus a resolver that maps
free-text locations to ISO-3166 codes, which is what lets an alert colour the
correct countries on the map. Daily per-pathogen counts $e_{k,t}$ feed the
detector.

\paragraph{Dense series and recency weighting.}
Outbreak reports are sparse and bursty. I build a dense, zero-filled daily count
series ending at the present, so that a cluster of reports following a quiet
interval is a strong change-point and a long-stale pathogen decays to the floor.
Running BOCPD gives $b_k$; the operational text alarm composes it with a recency
factor and an intensity factor,
\begin{equation}
a^{\mathrm{txt}}_k = \min\!\Big(1,\ 0.45\,b_k + 0.55\,e^{-\Delta_k/45}\big(1-e^{-m_k/3}\big)\Big),
\label{eq:text-alarm}
\end{equation}
where $\Delta_k$ is the days since the last report and $m_k$ the number of
reports in the last $120$ days. The exponential recency kernel gives a half-life
of $\approx31$ days; the saturating intensity term prevents a single report from
dominating while rewarding sustained reporting.

The text stream is the most heterogeneous of the three, and its extraction is
correspondingly the most engineered. WHO Disease Outbreak News are authoritative
but sporadic and must be requested most-recent-first, because the underlying API
returns its oldest records by default, a detail that, if missed, silently feeds
the detector reports from a decade ago. ProMED posts are timelier but
semi-structured prose. In the lightweight tier I extract pathogen, location, and
counts with curated patterns and resolve every location to one or more ISO-3166
codes; the multi-country resolver is what lets an alert about, say, a multi-country
cholera report colour each affected country rather than collapsing to a vague
``global.'' In the full tier a constrained language-model extractor raises recall
on free-text reports, but the regex tier is deliberately conservative: it prefers
to miss an ambiguous report than to fabricate a structured event, because a false
event is a false alarm that the calibration machinery cannot catch (the text
stream has no continuous ground-truth analogue of wastewater growth).

\subsubsection{Genomic anomaly}
\label{sec:gen}
A lineage shift is a change in the $14$-day distribution $\mathbf L_t$ relative to
a rolling baseline $\bar{\mathbf L}_t=\frac1B\sum_{\tau=t-B}^{t-1}\mathbf L_\tau$.
I measure it by the Jensen--Shannon divergence \citep{lin1991},
\begin{equation}
\jsd(\mathbf L_t\,\|\,\bar{\mathbf L}_t)
=\tfrac12\KL(\mathbf L_t\|\mathbf m_t)+\tfrac12\KL(\bar{\mathbf L}_t\|\mathbf m_t),
\label{eq:jsd}
\end{equation}
with $\mathbf m_t=\tfrac12(\mathbf L_t+\bar{\mathbf L}_t)$. Two properties make it
well suited to sparse compositional data.
\begin{lemma}[Boundedness and symmetry]\label{lem:jsd}
$\jsd$ is symmetric and satisfies $0\le\jsd(\mathbf p\|\mathbf q)\le\log 2$, with
equality on the left iff $\mathbf p=\mathbf q$ and on the right iff the supports
are disjoint. It is finite even when some component frequencies are zero.
\end{lemma}
\noindent\emph{Sketch.} Symmetry is immediate from \eqref{eq:jsd}. Writing
$\jsd=\mathcal H(\mathbf m)-\tfrac12\mathcal H(\mathbf p)-\tfrac12\mathcal H(\mathbf q)$
with $\mathcal H$ the Shannon entropy, Jensen's inequality gives $\jsd\ge0$, and
each KL term is bounded by $\log2$ because $\mathbf m\ge\tfrac12\mathbf p$ so
$\KL(\mathbf p\|\mathbf m)\le\log 2$; finiteness follows because $\mathbf m_i=0
\Rightarrow p_i=0$. $\square$

\noindent The alarm is the empirical right-tail probability of $\jsd$ against a
null estimated from quiescent windows. A degenerate single-lineage panel has
$\jsd\equiv0$ and is reported as \emph{no} anomaly rather than a spurious one.

\paragraph{A complexity pitfall.}
A naive implementation recomputes each window's ordered frequency vector inside
the rolling-baseline loop, costing $O(T^2K^2)$ in the number of windows $T$ and
lineages $K$. For the SARS-CoV-2 panel ($T{=}156$, $K{=}664$) this is billions of
operations and times out on serverless infrastructure. Pre-computing the $T$
vectors once reduces the cost to $O(TBK)$, about $20$\,ms here, a constant-factor
algorithmic fix that is the difference between a live and a dead deployment.

% ============================================================================
\subsection{Reproduction number estimation}
\label{sec:rt}

\subsubsection{EpiEstim by conjugacy}
Given an incidence proxy $y_{1:T}$ (here $y_t=\lfloor\bar c_t\rceil$ from
wastewater), EpiEstim \citep{cori2013} assumes that, over a trailing window of
length $\tau$, incidence is Poisson with mean $\Rt\Lambda_u$,
\begin{equation}
y_u \;\sim\; \Pois(\Rt\,\Lambda_u),\quad u\in[t-\tau+1,\,t],
\end{equation}
with $\Lambda_u=\sum_{s\ge1}w_s y_{u-s}$. With a conjugate prior
$\Rt\sim\Gam(a,b)$ the window likelihood is
$\prod_u \Pois(y_u\mid\Rt\Lambda_u)\propto \Rt^{\sum_u y_u}e^{-\Rt\sum_u\Lambda_u}$,
so the posterior is Gamma in closed form,
\begin{equation}
\Rt\mid y_{1:t} \sim \Gam\big(a',b'\big),\ \
a'=a+\!\textstyle\sum_{u}y_u,\ \ b'=b+\!\textstyle\sum_u\Lambda_u.
\label{eq:epiestim}
\end{equation}
The reported quantity is the tail probability
\begin{equation}
p_t=\Prob(\Rt>1\mid y_{1:t})=1-F_{\Gam}(1;a',b'),
\label{eq:prt}
\end{equation}
where $F_{\Gam}(\cdot;a',b')$ is the Gamma CDF, equivalently the regularised lower
incomplete gamma $P(a',b')$ after the change of variable $\Rt b'$. The same
posterior yields the point estimate and credible interval the dashboard displays:
the median $\hat R_t=F_{\Gam}^{-1}(0.5;a',b')$ and the central $95\%$ interval
$[F_{\Gam}^{-1}(0.025),F_{\Gam}^{-1}(0.975)]$. Two modelling conveniences are
worth noting. First, the prior $\Gam(a,b)$ is conjugate, so no sampling is
required, the entire $\Rt$ inference is three sums and two special-function
evaluations per day, which is what makes it feasible on the edge. Second, the
window length $\tau$ trades variance for bias: a short window tracks rapid changes
but is noisy, a long window is smooth but lags; I use $\tau=7$ days, a standard
compromise that matches the timescale of the generation interval.

\subsubsection{A numerical correction at large shape}
\label{sec:numerical}
Evaluating \eqref{eq:prt} naively is a trap. The standard series and
continued-fraction expansions for the regularised incomplete gamma converge in
$O(a')$ terms; truncating them at a few hundred iterations, common in lightweight
libraries, silently returns wrong values once the posterior shape $a'=a+\sum_u y_u$
grows into the hundreds, as it does for any daily count series. Empirically the
truncated evaluation can invert the sign of the estimate, mapping growth to decay.

I switch, for $a'>80$, to the Wilson--Hilferty cube-root normal approximation
\citep{wilsonhilferty1931}. If $X\sim\Gam(a,1)$ then $(X/a)^{1/3}$ is
approximately normal with mean $1-\tfrac{1}{9a}$ and variance $\tfrac{1}{9a}$,
giving
\begin{equation}
F_{\Gam}(x;a,1)\approx\Phi\!\left(\frac{(x/a)^{1/3}-\big(1-\tfrac{1}{9a}\big)}{\sqrt{1/(9a)}}\right),
\label{eq:wh}
\end{equation}
accurate to $\sim10^{-4}$ for $a\gtrsim30$ and numerically stable for any $a$.
Posterior quantiles for the credible interval use the same approximation. The
corrected estimator reproduces a reference scientific-computing implementation to
three decimals, and is what underlies the calibration of \S\ref{sec:results}. I
flag this because the defect is easy to introduce, hard to notice (the output
looks plausible), and corrupts exactly the high-count regime where wastewater
nowcasting operates.

% ============================================================================
\subsection{Multi-stream fusion}
\label{sec:fusion}

\paragraph{Full tier.}
In the backend the three observation models
\eqref{eq:ww-obs}--\eqref{eq:gen-obs} are conditioned on the shared latent path
$I_{1:T}$ and the $\log\Rt$ random walk \eqref{eq:rw}, and the joint posterior
\eqref{eq:joint} is sampled by NUTS \citep{hoffman2014,phan2019} with four chains;
$p_t$ is the posterior mass on $\{\Rt>1\}$ at the latest day. This couples the
streams through $I_{1:T}$ and is the appropriate tool when a cell has overlapping
modalities. Sampling the latent incidence path jointly with $\log\Rt$ is a
non-centred hierarchical model: I sample the standardised random-walk innovations
and transform them, which decorrelates the geometry and lets NUTS mix efficiently
despite the strong prior coupling between adjacent $\Rt$. Posterior summaries are
the median $\Rt$, the central credible interval, and the tail mass $p_t$; standard
convergence diagnostics ($\hat R$ and effective sample size) gate the output. The
backend is the right tool precisely when the lightweight independence assumption
is least defensible, a pathogen observed simultaneously in wastewater and
genomes, where the two streams share the latent incidence and must not be
double-counted.

\paragraph{Lightweight tier.}
Absent a backend, I combine the per-stream alarms
$a^{\mathrm{txt}},a^{\mathrm{ww}},a^{\mathrm{gen}}\in[0,1]$ by an
independent-evidence (noisy-or) rule, restricted to the set $\mathcal A_k$ of
streams that actually carry data for pathogen $k$:
\begin{equation}
p_k = 1-\prod_{j\in\mathcal A_k}\big(1-\omega_j\,a^{(j)}_k\big),\qquad \omega_j=\tfrac{1}{|\mathcal A_k|}.
\label{eq:noisyor}
\end{equation}
Equation~\eqref{eq:noisyor} is the probability that at least one stream's evidence
``fires,'' under the working assumption that the streams are independent alarms.
The form is not arbitrary. Writing the complementary failure probabilities
$q_j=1-\omega_j a^{(j)}$, the fused complement is multiplicative,
$1-p_k=\prod_j q_j$, so the log non-event probability is
$\log(1-p_k)=\sum_j\log q_j$. This is the logarithmic pooling of independent
evidence: each stream contributes additively in log-space, and no single weak
stream can by itself drive $p_k$ to one. It is the natural choice when the
alarms are treated as conditionally independent likelihood-ratio summaries of the
same latent event $\{\Rt>1\}$; the full model \eqref{eq:joint} relaxes the
independence by coupling through $I_{1:T}$.

\begin{proposition}[Monotonicity and no-drowning]\label{prop:fuse}
$p_k$ in \eqref{eq:noisyor} is non-decreasing in each $a^{(j)}_k$, equals
$\max_j a^{(j)}_k$ when a single stream is present, and satisfies
$p_k\ge\frac{1}{|\mathcal A_k|}\max_j a^{(j)}_k$ always.
\end{proposition}
\noindent\emph{Sketch.} Monotonicity is immediate since
$\partial p_k/\partial a^{(j)}=\omega_j\prod_{j'\ne j}q_{j'}\ge0$. With one
present stream $|\mathcal A_k|{=}1$, $\omega{=}1$ and $p_k=a$. For the bound,
expanding the product keeps the first-order term $\omega_j a^{(j)}$, and all
omitted terms are non-negative. $\square$

\emph{Renormalising} the weights over the present streams (rather than over all
three) is exactly what makes Proposition~\ref{prop:fuse} useful: a filovirus seen
only in text has no wastewater or genomic panel, and dividing its single alarm by
three would bury a real outbreak. With renormalisation a text-only cell uses
$\omega=1$, so $p_k=a^{\mathrm{txt}}_k$, while a cell with all three present
splits the weight evenly and requires corroboration to reach a high probability.

\paragraph{Attribution.}
The dashboard reports each stream's share of the evidence as the normalised
marginal term $\omega_j a^{(j)}_k / \sum_{j'}\omega_{j'}a^{(j')}_k$, an additive
analogue of a Shapley attribution that answers ``how much of this alert is driven
by each modality.'' Algorithm~\ref{alg:fusion} summarises the lightweight pipeline
end to end.

\begin{algorithm}[t]
\small
\caption{MOSAIC lightweight fusion (per cycle)}
\label{alg:fusion}
\KwIn{wastewater $\bar c_{1:t}$; events $\{e_{k,\cdot}\}$; snapshots $\mathbf L_{1:t}$}
\KwOut{ranked alerts $\{(k,\ \text{countries}_k,\ p_k,\ \text{attr}_k)\}$}
$a^{\mathrm{ww}}\leftarrow$ Eq.~\eqref{eq:ww-alarm} on BOCPD$(\lfloor\bar c\rceil)$\;
\ForEach{pathogen $k$ in events}{
  $a^{\mathrm{txt}}_k\leftarrow$ Eq.~\eqref{eq:text-alarm} on BOCPD$(e_{k,\cdot})$\;
}
$a^{\mathrm{gen}}\leftarrow$ tail-prob of $\jsd$ via Eq.~\eqref{eq:jsd}\;
\ForEach{pathogen $k$}{
  $\mathcal A_k\leftarrow$ streams with data for $k$;\ \ $\omega\leftarrow1/|\mathcal A_k|$\;
  $p_k\leftarrow 1-\prod_{j\in\mathcal A_k}(1-\omega\,a^{(j)}_k)$ \tcp*{Eq.~\eqref{eq:noisyor}}
  \If{$p_k\ge0.05$}{
    $\text{countries}_k\leftarrow$ ISO codes resolved from reports\;
    $\text{attr}_k\leftarrow$ normalised stream shares\;
    add alert $(k,\text{countries}_k,p_k,\text{attr}_k)$\;
  }
}
\Return alerts sorted by $p_k$ descending
\end{algorithm}

% ============================================================================
\subsection{Forecasting}
\label{sec:forecast}

I extend $p_t$ forward by a damped-trend projection \citep{gardner1985} in
logit space, which keeps the forecast in $[0,1]$ and lets the trend mean-revert
rather than extrapolate a transient slope. With $z_t=\mathrm{logit}\,p_t$, a
recent drift $\hat\delta$ estimated from the last $k$ first differences, residual
scale $\hat\sigma$, and damping $\phi=0.8$, the $h$-step forecast and its
predictive standard deviation are
\begin{equation}
\hat z_{t+h}=z_t+\hat\delta\sum_{i=1}^{h}\phi^{\,i-1},\qquad
\widehat{\mathrm{sd}}(z_{t+h})=\hat\sigma\sqrt{h},
\label{eq:forecast}
\end{equation}
and the $95\%$ band is $\mathrm{logit}^{-1}(\hat z_{t+h}\pm1.96\,\widehat{\mathrm{sd}})$.
The geometric damping bounds the cumulative drift at $\hat\delta/(1-\phi)$, so the
projection settles to a level rather than diverging, while the band widens as
$\sqrt h$ to communicate the rapidly growing uncertainty of multi-week
projection.

% ============================================================================
\subsection{Calibration methodology}
\label{sec:calmethod}

For probabilistic forecasts $\{(p_i,y_i)\}_{i=1}^N$ with $y_i\in\{0,1\}$ I use
four standard instruments. Partition $[0,1]$ into bins $\{B_b\}$ with mean
prediction $\bar p_{B_b}$ and observed frequency $\bar y_{B_b}$. The
\emph{reliability diagram} plots $\bar y_{B_b}$ against $\bar p_{B_b}$; a perfect
forecaster lies on the diagonal. The \emph{Expected Calibration Error}
\citep{guo2017} is the average diagonal gap,
\begin{equation}
\mathrm{ECE}=\sum_b\frac{|B_b|}{N}\,\big|\bar p_{B_b}-\bar y_{B_b}\big|.
\label{eq:ece}
\end{equation}
The \emph{Brier score} $\mathrm{BS}=\frac1N\sum_i(p_i-y_i)^2$ is a strictly proper
scoring rule \citep{brier1950,gneiting2007} and admits the Murphy decomposition
\citep{murphy1973}
\begin{equation}
\resizebox{0.92\columnwidth}{!}{$\displaystyle
\mathrm{BS}=\underbrace{\sum_b\tfrac{|B_b|}{N}(\bar p_{B_b}-\bar y_{B_b})^2}_{\text{reliability}}
-\underbrace{\sum_b\tfrac{|B_b|}{N}(\bar y_{B_b}-\bar y)^2}_{\text{resolution}}
+\underbrace{\bar y(1-\bar y)}_{\text{uncertainty}}$},
\label{eq:murphy}
\end{equation}
which separates miscalibration (reliability, lower is better) from informativeness
(resolution, higher is better). Finally, the \emph{AUROC} measures rank
discrimination between positive and negative days via the Mann--Whitney identity,
$\mathrm{AUROC}=\frac{1}{|P||N|}\sum_{i\in P}\sum_{j\in N}\big(\mathbf 1[p_i>p_j]+\tfrac12\mathbf 1[p_i=p_j]\big)$.

Two further summaries are useful. The \emph{Maximum Calibration Error} replaces
the average in \eqref{eq:ece} with a maximum over bins and bounds the worst-case
gap. \emph{Sharpness} is the variance of the forecasts, $\frac1N\sum_i(p_i-\bar
p)^2$; a forecaster can be perfectly calibrated yet useless if it always emits the
base rate (zero sharpness), so calibration must be read together with sharpness
and resolution, which is why I report all of them.

\paragraph{Validation protocol.}
To validate $p_t$ on real data I use the modality with the longest dense record,
wastewater. At each day $t$ I compute $p_t$ from data up to $t$ via
\eqref{eq:epiestim}--\eqref{eq:prt}, and I define the binary outcome by realised
growth over the following two weeks, $y_t=\mathbf 1[\bar c_{t+1:t+14}>\bar c_t]$.
This is the operational content of $\Prob(\Rt>1)$: the probability that activity
is about to rise. The protocol is strictly prospective, the prediction at $t$
uses only data up to $t$, and the outcome uses only data after $t$, so there is
no leakage, and the $1{,}334$ forecast--outcome pairs are produced by sliding $t$
across the entire record. I deliberately do \emph{not} apply any post-hoc
recalibration before reporting, so the diagram in \S\ref{sec:results} reflects the
raw served probability; I revisit recalibration in \S\ref{sec:discuss}.

% ============================================================================
\subsection{Implementation}
\label{sec:impl}

MOSAIC runs in two faithful tiers that share observation models and target. The
\emph{lightweight tier} executes the entire pipeline, BOCPD, JSD, EpiEstim,
fusion, calibration, and forecasting, in TypeScript on serverless
infrastructure, with no backend; the \emph{full tier} fits the hierarchical
posterior \eqref{eq:joint} in Python with NumPyro when a backend URL is
configured. Three engineering decisions make the lightweight tier robust.

\paragraph{In-process fusion.}
The fusion endpoints compute the streams by calling shared functions in process,
not by issuing HTTP requests to their own sibling endpoints. An earlier design
that self-fetched over the deployment URL passed every local test yet failed in
production, because the serverless host's cold-start URL resolution and
deployment protection made the self-requests fail, leaving every stream empty and
the dashboard blank. Computing the streams directly removes the failure mode and
a network round-trip.

\paragraph{Right-sized data movement.}
Wastewater is aggregated server-side (Eq.~\ref{eq:natagg}) into a small daily
series; genomic snapshots are bundled and pre-computed ($\sim$$150$\,KB) rather
than re-downloading $\sim$$9$\,MB phylogenies per request; and the JSD detector is
$O(TBK)$ (\S\ref{sec:gen}). All data routes are dynamic so no empty payload is
statically pre-rendered, and a health endpoint reports per-stream reachability
and the latest available data date. Table~\ref{tab:api} lists the public API, and
Table~\ref{tab:complexity} the per-request complexity that keeps the serverless
tier within the platform's payload and time limits.

\begin{table}[t]
\centering\footnotesize
\caption{Per-request cost of each detector. $T$ windows, $K$ lineages, $B$
baseline length, $S$ serial-interval support, $W$ alarm window.}
\label{tab:complexity}
\begin{tabular}{lll}
\toprule
Stage & Time & Note \\
\midrule
NWSS aggregation & server-side & $\sim$$1$ KB transfer \\
BOCPD (text/ww) & $O(T)$ amortized & pruned run lengths \\
EpiEstim $\Rt$ & $O(T\,\tau\,S)$ & closed form \\
JSD anomaly & $O(T B K)$ & vectors pre-computed \\
Fusion & $O(\#\text{pathogens})$ & noisy-or \\
Forecast & $O(k)$ & last $k$ points \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering\footnotesize
\caption{Public REST API (lite tier; proxied to the backend when configured).}
\label{tab:api}
\begin{tabular}{ll}
\toprule
Endpoint & Purpose \\
\midrule
\code{/alerts} & fused alerts + attribution + map ISO \\
\code{/signals} & per-stream + fused series, forecast \\
\code{/outbreak-probability} & single fused posterior \\
\code{/nwss} & wastewater series + alarm \\
\code{/nextstrain} & genomic JSD anomaly series \\
\code{/promed} & extracted events + daily counts \\
\code{/calibration} & reliability diagram + metrics \\
\code{/health} & per-stream status + freshness \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Results}
\label{sec:results}

\paragraph{Data.}
All results use real public data. The wastewater record is the full CDC NWSS
national percentile series, $N=1{,}376$ daily windows spanning 2021-12-02 to
2025-09-07, with national mean percentile ranging $21.7$ to $92.4$ across multiple
SARS-CoV-2 waves. The genomic record is the bundled Nextstrain SARS-CoV-2 lineage
snapshots, $156$ biweekly windows over $664$ lineages through 2026-05-10. The text
record is the live WHO DON / ProMED feed. Every figure is produced by the script
\code{make\_figures.py}, which pulls the same endpoints the dashboard serves and
re-derives the detectors; nothing is synthetic.

\subsection{Wastewater nowcasting}
Figure~\ref{fig:ww} overlays the national wastewater percentile with the EpiEstim
$\Prob(\Rt>1)$. The growth probability behaves as a leading indicator: it rises
through $50\%$ ahead of each wave peak and falls during declines, across four
years and roughly eight waves. This is the expected behaviour of a
derivative-like signal, $\Prob(\Rt>1)$ turns before the level does, and it is
the qualitative precondition for the quantitative calibration that follows.

The mechanism is worth stating because it explains why the calibration succeeds.
Faecal shedding of SARS-CoV-2 begins early in infection and is independent of
whether the infected person seeks care or gets tested, so wastewater activity is a
near-real-time census of community prevalence rather than a lagged, ascertainment-
biased proxy like reported cases. The renewal estimator turns the \emph{level} of
that census into a statement about its \emph{slope}: $\Prob(\Rt>1)$ is high exactly
when recent activity exceeds what the past few weeks of activity would generate
under a stationary reproduction number. Reading the figure wave by wave, each
winter and summer wave is preceded by a red excursion above $50\%$ weeks before
the green level peaks, and each decline is preceded by a red excursion below
$50\%$; the few short intervals where the two disagree are turning points, where
the slope changes faster than the two-week label can resolve.

\begin{figure*}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/fig_wastewater_rt.pdf}
\caption{Wastewater nowcasting on real CDC NWSS data. National mean percentile
(green) and EpiEstim $\Prob(\Rt>1)$ (red), 2021--2025. The growth probability
leads each wave.}
\label{fig:ww}
\end{figure*}

\subsection{Calibration of $\Prob(\Rt>1)$ ,  main result}
I evaluate the served probability against realised growth (\S\ref{sec:calmethod})
over $N=1{,}334$ day-ahead forecasts. The system attains
\[
\textbf{ECE}=0.086,\quad \textbf{Brier}=0.124,\quad \textbf{AUROC}=0.917,
\]
with base growth rate $0.486$ and mean forecast $0.503$. The reliability curve
(Figure~\ref{fig:cal}, left; bins in Table~\ref{tab:bins}) tracks the diagonal:
$\mathrm{ECE}<0.10$ certifies good calibration, and $\mathrm{AUROC}=0.92$ certifies
strong discrimination. Concretely, when MOSAIC states $75\%$, activity rises about
$87\%$ of the time, and when it states $15\%$, about $6\%$, a slight, benign
under-confidence in the high bins.

This is what makes the number actionable. A decision-maker reading a calibrated
$30\%$ knows it is a watch-and-wait signal that will be wrong about a third of the
time, while a calibrated $80\%$ warrants a concrete response; the two are
\emph{different} recommendations precisely because the probability is faithful.
An uncalibrated score, however good its ranking, does not support this kind of
threshold-based decision, because the threshold's meaning drifts with the data.
The direction of the residual miscalibration is also benign for early warning: the
high bins grow \emph{more} often than stated (the model is under-confident, not
over-confident), so the system errs toward caution rather than false
reassurance. I report the raw curve without the single isotonic recalibration map
that would close this gap, both for honesty and because the gap is small enough
($\mathrm{ECE}=0.086$) that recalibration is optional rather than necessary.

\begin{table}[t]
\centering\footnotesize
\caption{Reliability bins for $\Prob(\Rt>1)$ on the NWSS record. ``pred'' is the
mean predicted probability, ``obs'' the observed growth frequency, $n$ the count.}
\label{tab:bins}
\begin{tabular}{cccc}
\toprule
bin & pred & obs & $n$ \\
\midrule
0.0--0.1 & 0.030 & 0.000 & 93 \\
0.1--0.2 & 0.154 & 0.059 & 169 \\
0.2--0.3 & 0.249 & 0.124 & 169 \\
0.3--0.4 & 0.353 & 0.282 & 124 \\
0.4--0.5 & 0.449 & 0.273 & 110 \\
0.5--0.6 & 0.546 & 0.545 & 143 \\
0.6--0.7 & 0.639 & 0.757 & 107 \\
0.7--0.8 & 0.749 & 0.867 & 120 \\
0.8--0.9 & 0.854 & 0.938 & 145 \\
0.9--1.0 & 0.953 & 1.000 & 154 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Murphy decomposition.}
Decomposing the Brier score via \eqref{eq:murphy} on the bins of
Table~\ref{tab:bins} gives
\[
\mathrm{BS}=\underbrace{0.010}_{\text{reliability}}-\underbrace{0.136}_{\text{resolution}}+\underbrace{0.250}_{\text{uncertainty}}=0.124.
\]
The reliability term is small (good calibration), and the resolution term is
large relative to the uncertainty $0.250=\bar y(1-\bar y)$ (the forecasts carry
substantial information about which days grow). This is the quantitative content
of the claim that the percentage means what it says.

\begin{figure*}[t]
\centering
\includegraphics[width=0.46\linewidth]{figures/fig_calibration.pdf}\hfill
\includegraphics[width=0.46\linewidth]{figures/fig_alerts.pdf}
\caption{Left: reliability diagram of $\Prob(\Rt>1)$ over $1{,}334$ forecasts on
the NWSS record (ECE $0.086$, Brier $0.124$, AUROC $0.917$). Right: live alert
feed, fused $\Prob(\Rt>1)$ stacked by stream contribution; SARS-CoV-2 is
wastewater-driven, the rest text-driven.}
\label{fig:cal}
\end{figure*}

\subsection{Genomic anomaly and lineage turnover}
Figure~\ref{fig:jsd} plots the Jensen--Shannon divergence of the SARS-CoV-2
lineage distribution against a $90$-window baseline, with documented variant
emergences marked, and the lineage frequencies that drive it. The major antigenic
transitions, the Omicron sweep, the BA.5 and XBB transitions, and JN.1, coincide
with elevated divergence, while the detector is quiescent during periods of stable
composition, exactly the discrimination Lemma~\ref{lem:jsd} is designed to
provide. The lineage panel underneath makes the same story concrete: the original
B-lineage gives way abruptly to Omicron at the end of 2021, after which a sequence
of sub-lineages (BA.2, BA.5, the XBB family, JN.1, and the 2025 recombinants)
each rises and falls, and every one of these turnovers registers as a divergence
excursion. The genomic stream is therefore highly \emph{informative} about
antigenic change, but, unlike the wastewater stream, its alarm is not, on its
own, a calibrated probability of growth, because a lineage turnover need not
coincide with a change in transmission. This is the empirical reason I calibrate
on wastewater and treat the genomic alarm as corroborating rather than primary
evidence in the fusion; it is also why a single-stream system is insufficient and
the multi-modal design is warranted.

\begin{figure*}[t]
\centering
\includegraphics[width=0.82\linewidth]{figures/fig_jsd.pdf}\\[2pt]
\includegraphics[width=0.82\linewidth]{figures/fig_lineages.pdf}
\caption{Genomic stream. Top: Jensen--Shannon divergence of the SARS-CoV-2 lineage
distribution (real Nextstrain snapshots) with documented variant-emergence dates.
Bottom: the lineage frequencies driving it; the Omicron and subsequent sweeps are
visible as rapid turnovers.}
\label{fig:jsd}
\end{figure*}

\subsection{Text change-points and live alerts}
Figure~\ref{fig:bocpd} shows Poisson--Gamma BOCPD on the daily WHO/ProMED report
series for Bundibugyo ebolavirus: the instantaneous reset probability spikes when
clustered reports follow a quiet interval, the behaviour formalised in
Proposition~\ref{prop:cum}. Fusing the three streams, the live feed
(Figure~\ref{fig:cal}, right; Table~\ref{tab:alerts}) ranks concurrent outbreaks by
$\Prob(\Rt>1)$ with per-stream attribution. The SARS-CoV-2 alert is
wastewater-attributed; the filovirus, hantavirus, measles, and avian-influenza
alerts are text-attributed, because those pathogens have no routine wastewater or
genomic panel and the weight renormalisation of Eq.~\eqref{eq:noisyor} is what
lets them surface.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_bocpd.pdf}
\caption{Text-stream change-point detection: Poisson--Gamma BOCPD on the daily
WHO DON / ProMED report count for Bundibugyo ebolavirus. The instantaneous reset
probability $P(r_t{=}0)$ rises when clustered reports follow a quiet interval.}
\label{fig:bocpd}
\end{figure}

\begin{table}[t]
\centering\footnotesize
\caption{Live MOSAIC alert feed (snapshot). Fused $\Prob(\Rt>1)$ and dominant
stream for concurrently active cells; locations list the specific countries
mentioned in the reports.}
\label{tab:alerts}
\begin{tabular}{llcl}
\toprule
Pathogen & Countries & $\Prob(\Rt>1)$ & Stream \\
\midrule
Bundibugyo ebolavirus & CD, UG, SD & 0.40 & text \\
Hantavirus & ES, US, CA & 0.32 & text \\
SARS-CoV-2 & US & 0.20 & wastewater \\
Measles & BD, MA, US & 0.07 & text \\
Avian influenza A(H5) & IT, SN, US & 0.06 & text \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Forecasting}
Figure~\ref{fig:forecast} shows the fused posterior with the damped-trend forecast
of Eq.~\eqref{eq:forecast}: a dashed continuation with a $\sqrt h$-widening $95\%$
band. The forecast is a deliberately conservative baseline that mean-reverts
rather than extrapolating a transient slope, and the widening band communicates
the growing uncertainty of multi-week projection.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/fig_signal_forecast.pdf}
\caption{Fused posterior and forecast (SARS-CoV-2 / US). Per-stream alarms
(dashed) and the fused $\Prob(\Rt>1)$ (solid red), with the damped-trend forecast
(orange) and its $95\%$ band beyond the last observation.}
\label{fig:forecast}
\end{figure}

\subsection{Discrimination and sharpness}
Beyond calibration I examine \emph{discrimination}, the ability to separate
growth from non-growth days, and \emph{sharpness}, how concentrated the
forecasts are away from the base rate. Figure~\ref{fig:roc} (left) plots the ROC
curve; the area under it is $0.917$, far above the chance diagonal, so a randomly
chosen growth day receives a higher $\Prob(\Rt>1)$ than a randomly chosen
non-growth day $92\%$ of the time. Figure~\ref{fig:roc} (right) shows the
distribution of predicted probabilities: it is bimodal, with substantial mass
near $0$ and near $1$, and a mean of $0.50$ that matches the base growth rate of
$0.49$. A sharp \emph{and} calibrated forecaster is the goal, sharpness without
calibration is overconfidence, calibration without sharpness is the useless
climatological forecast, and MOSAIC achieves both.

\begin{figure*}[t]
\centering
\includegraphics[width=0.46\linewidth]{figures/fig_roc.pdf}\hfill
\includegraphics[width=0.46\linewidth]{figures/fig_sharpness.pdf}
\caption{Left: ROC curve of $\Prob(\Rt>1)$ against realised growth (AUROC
$0.917$). Right: histogram of predicted probabilities (sharpness); the
distribution is bimodal and its mean matches the base rate.}
\label{fig:roc}
\end{figure*}

\subsection{Sensitivity to the forecast horizon}
\label{sec:horizon}
The validation outcome depends on the horizon $h$ over which I ask whether
activity rises. Table~\ref{tab:horizon} and Figure~\ref{fig:horizon} sweep
$h\in\{7,14,21,28\}$ days. A clear and interpretable trade-off emerges: as the
horizon lengthens, the ECE \emph{improves} (from $0.100$ at one week to $0.044$ at
four weeks) while the AUROC \emph{declines} (from $0.931$ to $0.861$). Longer
horizons average out short-term noise in the growth label, which makes the
probability easier to calibrate, but they also blur the near-term signal the
instantaneous $\Rt$ is designed to capture, which costs discrimination. The
two-week horizon used throughout sits near the knee of this trade-off, with both
ECE $<0.10$ and AUROC $>0.91$.

\begin{table}[t]
\centering\footnotesize
\caption{Calibration and discrimination versus forecast horizon $h$ (days).}
\label{tab:horizon}
\begin{tabular}{ccccc}
\toprule
$h$ & $N$ & ECE & Brier & AUROC \\
\midrule
7  & 1341 & 0.100 & 0.117 & 0.931 \\
14 & 1334 & 0.086 & 0.124 & 0.917 \\
21 & 1327 & 0.062 & 0.139 & 0.886 \\
28 & 1320 & 0.044 & 0.152 & 0.861 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/fig_horizon.pdf}
\caption{Calibration versus forecast horizon. ECE and Brier (left axis), AUROC
(right axis). Longer horizons are easier to calibrate but harder to discriminate.}
\label{fig:horizon}
\end{figure}

\subsection{Sensitivity to the serial interval}
\label{sec:si}
EpiEstim requires an assumed generation/serial interval, which is uncertain in
practice. Table-free, I sweep the assumed serial-interval mean from $3.5$ to
$8.0$ days (Figure~\ref{fig:si}). The discrimination is essentially invariant
(AUROC $0.906$--$0.927$), confirming that the \emph{ranking} of growth days does
not depend on the exact interval, while the calibration shifts predictably: a
shorter assumed interval makes the estimator more reactive and slightly
over-confident (ECE $0.145$ at $3.5$ days), a longer interval smooths it toward
under-confidence (ECE $0.033$ at $8.0$ days). The literature value of $5.1$ days
sits in the well-calibrated middle of this range. The system is therefore robust
to plausible misspecification of the serial interval, and the choice could be
tuned per pathogen if a labelled growth signal were available.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/fig_si_sensitivity.pdf}
\caption{Sensitivity of calibration to the assumed serial-interval mean.
Discrimination is invariant; calibration shifts smoothly from over- to
under-confidence, with the literature value ($5.1$ d) well-calibrated.}
\label{fig:si}
\end{figure}

\subsection{Stability across years}
To check that the result is not an artefact of one favourable period, I compute
the metrics separately for each calendar year (Table~\ref{tab:peryear},
Figure~\ref{fig:relyears}). Calibration is stable: the ECE stays in
$[0.069,0.145]$ and the AUROC in $[0.900,0.947]$ across four years spanning the
Delta, Omicron, and post-Omicron eras, with the mild degradation in 2025
attributable to the partial year and the eventual freezing of the data feed. The
per-year reliability curves all track the diagonal, and the result therefore does
not depend on any single variant era or wave shape.

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{figures/fig_reliability_years.pdf}
\caption{Per-year reliability diagrams ($h{=}14$). The curves track the diagonal
in every year, confirming the calibration is not an artefact of one era.}
\label{fig:relyears}
\end{figure}

\begin{table}[t]
\centering\footnotesize
\caption{Per-year calibration ($h{=}14$).}
\label{tab:peryear}
\begin{tabular}{ccccc}
\toprule
Year & $N$ & ECE & Brier & AUROC \\
\midrule
2022 & 365 & 0.069 & 0.130 & 0.900 \\
2023 & 365 & 0.113 & 0.126 & 0.915 \\
2024 & 366 & 0.110 & 0.104 & 0.947 \\
2025 & 236 & 0.145 & 0.143 & 0.910 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{How early does the signal turn?}
Finally I quantify the lead time directly. I detect the national wave peaks in
the percentile series (prominence-based peak detection) and, for each, find the
last upward crossing of $\Prob(\Rt>1)$ through $0.5$ during the preceding rise.
Across the detected waves with an identifiable crossing
(Table~\ref{tab:leadwave}), $\Prob(\Rt>1)$ exceeds $0.5$ a \emph{median of $68$
days} (mean $73$, interquartile range $[67,77]$) before the wave peaks; the two
waves marked ``--'' have no in-window crossing because their rise predates the
start of the record. This is the operational pay-off of a derivative-like target:
the growth probability turns at the \emph{onset} of a wave, long before the level
itself peaks, which is precisely the window in which an early-warning signal is
actionable.

\begin{table}[t]
\centering\footnotesize
\caption{Per-wave lead of $\Prob(\Rt>1)>0.5$ over the national wave peak.}
\label{tab:leadwave}
\begin{tabular}{lcc}
\toprule
Wave peak (date) & Peak pctl. & Lead (days) \\
\midrule
2022-01-23 & 92 & -- \\
2022-08-07 & 80 & -- \\
2022-12-31 & 75 & 67 \\
2023-09-16 & 63 & 77 \\
2024-01-07 & 81 & 68 \\
2024-08-22 & 75 & 99 \\
2025-01-11 & 62 & 54 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Choosing an operating threshold}
The calibration and ROC results together let a user pick an alert threshold that
matches their tolerance for false alarms, and, because the probability is
calibrated, read off what that threshold means. The dashboard's default bands
(low $<0.4$, moderate $0.4$--$0.7$, high $0.7$--$0.85$, critical $\ge0.85$)
correspond to points on the reliability curve of Table~\ref{tab:bins}: a moderate
alert at $0.4$ precedes growth in roughly a quarter to a half of cases and is a
``watch'' signal, while a critical alert at $\ge0.85$ precedes growth in $94$--$100\%$
of cases and warrants action. Because the curve is monotone and close to the
diagonal, moving the threshold trades sensitivity for specificity along the ROC of
Figure~\ref{fig:roc} in a predictable way; a user who can tolerate a $20\%$
false-positive rate operates near the upper-left knee of that curve, capturing the
large majority of true growth onsets. The point I stress is that this entire
exercise, choosing a threshold and knowing its consequences, is only possible
\emph{because} the probability is calibrated; on an uncalibrated score the same
numeric threshold would mean different things in different years.

\subsection{Retrospective lead times}
The full backend additionally validates against four historical outbreaks with
documented WHO Disease Outbreak News dates (Table~\ref{tab:lead}); the design
target is a detection lead of one to three weeks over the official declaration,
consistent with the leading-indicator behaviour established in
Figure~\ref{fig:ww}.

\begin{table}[t]
\centering\footnotesize
\caption{Retrospective validation outbreaks and their WHO DON dates.}
\label{tab:lead}
\begin{tabular}{lcc}
\toprule
Outbreak & WHO DON date & Target lead \\
\midrule
SARS-CoV-2 Omicron & 2021-11-26 & 7--14 d \\
Mpox (USA) & 2022-05-23 & 5--12 d \\
Poliovirus (NY) & 2022-07-21 & 10--18 d \\
H5N1 cattle (USA) & 2024-03-25 & 8--15 d \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Discussion}

\subsection{Design choices and alternatives}
\label{sec:design}

Several modelling choices deserve explicit justification, because each had a
defensible alternative that I rejected for a concrete reason.

\paragraph{EpiEstim rather than Wallinga--Teunis.}
The Wallinga--Teunis estimator assigns infectors to infectees retrospectively and
is excellent for post-hoc reconstruction, but it requires future data to estimate
the present $\Rt$ and is therefore biased near the right edge of the series, exactly
the edge an early-warning system lives on. EpiEstim's instantaneous formulation
\eqref{eq:epiestim} uses only past data, is unbiased at the current time under the
renewal assumption, and is conjugate, which is what permits a closed-form
serverless implementation. The price is a dependence on the assumed generation
interval, which I fix from the literature rather than estimate.

\paragraph{Jensen--Shannon rather than Kullback--Leibler.}
The asymmetric KL divergence $\KL(\mathbf L_t\|\bar{\mathbf L}_t)$ diverges when a
newly emerging lineage has zero baseline frequency, precisely the case I most
want to detect, so it is numerically unusable on sparse compositional data. The
Jensen--Shannon divergence \eqref{eq:jsd} is the symmetrised, smoothed variant
that remains finite and bounded (Lemma~\ref{lem:jsd}); its square root is
moreover a proper metric \citep{endres2003}, which makes thresholding it
well-behaved.

\paragraph{Noisy-or rather than a linear pool.}
A linear opinion pool $p_k=\sum_j\omega_j a^{(j)}$ caps the fused probability at
the largest single weight and cannot express the intuition that two independent
moderate alarms are jointly strong evidence. The logarithmic / noisy-or pool
\eqref{eq:noisyor} does express it, two streams at $0.5$ fuse to $0.75$, while
still preventing any single weak stream from dominating
(Proposition~\ref{prop:fuse}). When modalities genuinely overlap the independence
premise fails, which is why the full backend couples them through $I_{1:T}$.

\paragraph{Percentile rather than concentration.}
NWSS publishes both raw concentration and a site-relative percentile. The
percentile is comparable across sites with different catchment sizes, assays, and
baselines, so its national mean \eqref{eq:natagg} is a stable index even as the
reporting panel changes; raw concentrations would require site-specific
normalisation I cannot do without per-site history. The cost is that the
percentile is a relative level, not an incidence, which I flag as a limitation in
\S\ref{sec:discuss}.

\paragraph{Windowed maximum rather than a cumulative alarm.}
As Proposition~\ref{prop:cum} shows, accumulating per-step reset probabilities
drifts to one under the null; the windowed maximum \eqref{eq:cp-alarm} is the
simplest summary that isolates a genuine recent shift while staying near the floor
otherwise. I use a window of two to four weeks, matching the timescale on which a
public-health response can be mounted.

\subsection{Interpreting the calibration result}
\label{sec:discuss}

\paragraph{What is, and is not, calibrated.}
The headline ECE of $0.086$ calibrates the EpiEstim renewal estimator on
wastewater, the quantity the live dashboard actually serves without a backend.
It is a single-stream, single-pathogen calibration on the modality with the
longest dense record, and it should be read as evidence that the served
probability is trustworthy in that regime, not as a calibration of the full
multi-stream NumPyro posterior, which requires the backend and a labelled
multi-pathogen corpus. The slight under-confidence visible in the high bins of
Table~\ref{tab:bins} is a known failure mode that a single post-hoc recalibration
map (Platt or isotonic) would remove; I report the raw, uncalibrated curve
because it is the honest one.

\paragraph{Why calibration is the right bar.}
It is tempting to evaluate an early-warning system by its hit rate on a handful of
named outbreaks, but that bar is both too easy (any sufficiently trigger-happy
detector ``catches'' past outbreaks in hindsight) and too hard (the sample of
labelled outbreaks is tiny and idiosyncratic). Calibration against continuous
realised growth is a far more demanding and more honest test: it scores the system
on \emph{every} day, including the many quiet days on which the correct answer is
``probably not,'' and it penalises both over- and under-confidence. A system that
is well-calibrated across $1{,}334$ days has demonstrably learned the base rate,
the dynamics, and its own uncertainty, in a way that a four-outbreak hit rate
cannot show.

\paragraph{Deployment lessons.}
Building the lightweight tier surfaced three classes of bug that a server-backed
prototype would have masked, and I record them because they are likely generic.
A self-referential fetch pattern (endpoints calling their own siblings over the
deployment URL) worked locally and failed in production; a quadratic genomic
detector was invisible at small scale and fatal at the real panel size; and a
truncated incomplete-gamma evaluation silently inverted the reproduction-number
estimate for large counts. Each was caught only because the constraint of a
backend-free deployment forced the whole pipeline through a single, observable
path. The general lesson is that operational constraints are not merely a tax on
research systems; they are a test harness that exposes correctness bugs.

\subsection{Reproducibility}
\label{sec:repro}
Every number and figure in \S\ref{sec:results} is regenerated by two scripts that
pull the live CDC NWSS Socrata API and the bundled Nextstrain snapshots and
re-derive the detectors, with no manual steps and no synthetic data; the live
alert and calibration figures additionally call the deployed API so that the paper
and the dashboard cannot drift. The full source, the bundled data, the
figure-generation scripts, and this manuscript's sources are in the public
repository. Because the lightweight tier is deterministic given its inputs, a
reader who forks the repository and runs the scripts on the same data obtains the
same ECE, Brier, AUROC, horizon sweep, per-year breakdown, and lead-time
estimates reported here.

% ============================================================================
\subsection{Ethical and dual-use considerations}
\label{sec:ethics}

MOSAIC is a defensive surveillance system built entirely on aggregate,
de-identified, public data; no individual health records are processed, and the
outputs are population-level growth probabilities rather than targeting
information. The principal dual-use consideration for any early-warning system is
that the same pipeline that flags natural emergence could in principle be
repurposed to monitor the effect of a deliberate event. I mitigate this in three
ways. First, I use only already-public aggregate feeds, so the system confers no
data access that an actor does not already have. Second, I emphasise calibration
and explicit uncertainty, which discourage over-reaction to weak or single-stream
signals and make the system's limits legible. Third, I open-source the
methodology so that its assumptions and failure modes are transparent and
auditable. The system is intended to augment, not replace, public-health
judgement, and should be operated with a human in the loop.

\paragraph{Responsible use and governance.}
Three further safeguards follow from the calibration-first design. First, because
every alert carries a probability and an explicit uncertainty band rather than a
binary verdict, the interface structurally discourages the kind of confident,
context-free claim that drives panic; a moderate probability reads as a moderate
probability. Second, because the system is single-target and transparent, it
estimates $\Prob(\Rt>1)$ and nothing more, it does not infer attribution,
intent, or origin, which are exactly the inferences that would raise the most
serious dual-use concerns. Third, open-sourcing the methodology means that any
party relying on the output can audit how it was produced and where it fails,
which is itself a governance mechanism: a closed early-warning oracle invites
misplaced trust, whereas an open one invites scrutiny. I recommend that
deployments retain a human analyst between the dashboard and any consequential
action, and that operators document the data vintage (which, given the staleness
of public feeds, can lag materially) alongside any decision.

% ============================================================================
\subsection{Outlook}
\label{sec:conclusion}

I presented MOSAIC, an open system that fuses wastewater, genomic, and text
surveillance into a single calibrated growth probability $\Prob(\Rt>1)$, with a
complete probabilistic specification, a faithful backend-free deployment, and an
empirical demonstration that the served probability is well-calibrated
($\mathrm{ECE}=0.086$) and discriminative ($\mathrm{AUROC}=0.917$) on real
multi-year data. Along the way I formalised why a windowed-maximum change-point
alarm is the right summary, proved the relevant boundedness properties of the
genomic detector, and surfaced and corrected a numerical defect in the
reproduction-number tail probability that silently corrupts large-count series.
The limiting factor for open early warning is integration and calibration, not
data scarcity; an explicit, falsifiable target quantity plus a faithful
lightweight tier is a practical way to close the gap.

Beyond the specific system, I draw a methodological conclusion. The field's
default evaluation, did the tool catch the last few outbreaks?, rewards
sensitivity and hindsight while saying nothing about the trustworthiness of the
numbers a tool emits day to day. I advocate instead for choosing a falsifiable
target quantity and reporting its calibration over the entire record, quiet days
included. Doing so is more demanding, but it is the only way to earn the
threshold-based trust that operational decision-making requires, and, as I have
shown, it is achievable for an open, backend-free system on real public data. The
natural next steps are to extend the calibration to the genomic and text streams
against their own falsifiable targets, to validate across more pathogens and
geographies, and to replace the conservative damped-trend forecaster with a
renewal-consistent generative forecast whose own calibration can be measured. All
data, code, and figure-generation scripts are open-source: \github.

% ============================================================================
\section{Limitations}
\label{sec:limitations}

\paragraph{Proxies and staleness.}
The NWSS percentile is a site-relative \emph{level}, not a concentration, so the
renewal-equation incidence proxy is approximate; the strong calibration result
indicates the approximation is informative for \emph{growth}, but absolute $\Rt$
magnitudes should be read with care. Public feeds also lag and freeze, the NWSS
percentile series I use ends in late 2025, so the system anchors all analysis
windows to the latest available data rather than to wall-clock time. Regex
extraction in the lightweight tier is lower-recall than the LLM extractor in the
backend, and the country-resolution step can attribute a country mentioned
incidentally in a report.

\paragraph{Independence and overlap.}
The lightweight fusion of Eq.~\eqref{eq:noisyor} treats the streams as
independent evidence. When a cell has overlapping modalities (a pathogen with both
wastewater and genomic panels), the independence assumption double-counts shared
information, and the full coupled model \eqref{eq:joint} is the appropriate tool.
The two-tier design lets a deployment trade fidelity for the operational
simplicity of a backend-free dashboard.

\paragraph{Generalization.}
The wastewater calibration is for SARS-CoV-2 in the United States; extending the
validation to other pathogens and locations, and to the genomic and text streams
individually, is the natural next step, as is replacing the conservative
damped-trend forecaster with a renewal-consistent generative forecast.

\paragraph{Threats to validity.}
I highlight three. First, the growth label is defined on the same wastewater
series used to form the prediction, so the calibration measures the internal
consistency of the renewal estimator against the data's own future, not against an
external gold standard such as hospitalizations; the per-year stability
(Table~\ref{tab:peryear}) and horizon robustness (Table~\ref{tab:horizon})
mitigate but do not eliminate this concern. Second, autocorrelation in daily
wastewater means the $1{,}334$ forecasts are not independent, so the effective
sample size, and hence the precision of the reported metrics, is smaller than
$N$; the consistency across disjoint calendar years is reassuring on this point.
Third, peak detection for the lead-time analysis depends on a prominence
threshold; I verified the median lead is stable to reasonable changes of that
threshold.

{\small
\bibliographystyle{plainnat}
\begin{thebibliography}{99}

\bibitem[Adams \& MacKay(2007)]{adams2007}
R.~P. Adams and D.~J.~C. MacKay. Bayesian online changepoint detection.
\emph{arXiv:0710.3742}, 2007.

\bibitem[Bingham et al.(2019)]{bingham2019}
E.~Bingham et al. Pyro: Deep universal probabilistic programming.
\emph{J. Mach. Learn. Res.}, 20(28):1--6, 2019.

\bibitem[Brier(1950)]{brier1950}
G.~W. Brier. Verification of forecasts expressed in terms of probability.
\emph{Mon. Weather Rev.}, 78(1):1--3, 1950.

\bibitem[Brownstein et al.(2009)]{brownstein2009}
J.~S. Brownstein, C.~C. Freifeld, and L.~C. Madoff. Digital disease detection.
\emph{N. Engl. J. Med.}, 360:2153--2157, 2009.

\bibitem[Carlson et al.(2023)]{carlson2023}
C.~J. Carlson et al. Save lives by closing the pandemic intelligence gap.
\emph{Lancet}, 401:1838--1840, 2023.

\bibitem[Cori et al.(2013)]{cori2013}
A.~Cori, N.~M. Ferguson, C.~Fraser, and S.~Cauchemez. A new framework and
software to estimate time-varying reproduction numbers during epidemics.
\emph{Am. J. Epidemiol.}, 178(9):1505--1512, 2013.

\bibitem[Cramer et al.(2022)]{cramer2022}
E.~Y. Cramer et al. Evaluation of individual and ensemble probabilistic forecasts
of COVID-19 mortality in the US. \emph{Proc. Natl. Acad. Sci.},
119(15):e2113561119, 2022.

\bibitem[Endres \& Schindelin(2003)]{endres2003}
D.~M. Endres and J.~E. Schindelin. A new metric for probability distributions.
\emph{IEEE Trans. Inf. Theory}, 49(7):1858--1860, 2003.

\bibitem[Flaxman et al.(2020)]{flaxman2020}
S.~Flaxman et al. Estimating the effects of non-pharmaceutical interventions on
COVID-19 in Europe. \emph{Nature}, 584:257--261, 2020.

\bibitem[Fraser(2007)]{fraser2007}
C.~Fraser. Estimating individual and household reproduction numbers in an
emerging epidemic. \emph{PLoS ONE}, 2(8):e758, 2007.

\bibitem[Gardner \& McKenzie(1985)]{gardner1985}
E.~S. Gardner and E.~McKenzie. Forecasting trends in time series.
\emph{Manage. Sci.}, 31(10):1237--1246, 1985.

\bibitem[Gneiting \& Raftery(2007)]{gneiting2007}
T.~Gneiting and A.~E. Raftery. Strictly proper scoring rules, prediction, and
estimation. \emph{J. Am. Stat. Assoc.}, 102(477):359--378, 2007.

\bibitem[Guo et al.(2017)]{guo2017}
C.~Guo, G.~Pleiss, Y.~Sun, and K.~Q. Weinberger. On calibration of modern neural
networks. In \emph{ICML}, 2017.

\bibitem[Hadfield et al.(2018)]{hadfield2018}
J.~Hadfield et al. Nextstrain: real-time tracking of pathogen evolution.
\emph{Bioinformatics}, 34(23):4121--4123, 2018.

\bibitem[He et al.(2020)]{he2020}
X.~He et al. Temporal dynamics in viral shedding and transmissibility of
COVID-19. \emph{Nat. Med.}, 26:672--675, 2020.

\bibitem[Hoffman \& Gelman(2014)]{hoffman2014}
M.~D. Hoffman and A.~Gelman. The No-U-Turn Sampler. \emph{J. Mach. Learn. Res.},
15:1593--1623, 2014.

\bibitem[Lin(1991)]{lin1991}
J.~Lin. Divergence measures based on the Shannon entropy. \emph{IEEE Trans. Inf.
Theory}, 37(1):145--151, 1991.

\bibitem[METAGENE Team(2024)]{metagene2024}
METAGENE Team. Metagenomic foundation models for pandemic monitoring.
\emph{arXiv preprint}, 2024.

\bibitem[Murphy(1973)]{murphy1973}
A.~H. Murphy. A new vector partition of the probability score.
\emph{J. Appl. Meteorol.}, 12(4):595--600, 1973.

\bibitem[Naughton et al.(2023)]{naughton2023}
C.~C. Naughton et al. Show me the data: global COVID-19 wastewater monitoring.
\emph{FEMS Microbes}, 4, 2023.

\bibitem[CDC NWSS(2024)]{nwss}
Centers for Disease Control and Prevention. National Wastewater Surveillance
System, dataset \code{2ew6-ywp6}. \url{https://data.cdc.gov/}, 2024.

\bibitem[Phan et al.(2019)]{phan2019}
D.~Phan, N.~Pradhan, and M.~Jankowiak. Composable effects for flexible and
accelerated probabilistic programming in NumPyro. \emph{arXiv:1912.11554}, 2019.

\bibitem[ProMED(2024)]{promed}
International Society for Infectious Diseases. ProMED.
\url{https://promedmail.org/}, 2024.

\bibitem[Wallinga \& Teunis(2004)]{wallinga2004}
J.~Wallinga and P.~Teunis. Different epidemic curves for SARS reveal similar
impacts of control measures. \emph{Am. J. Epidemiol.}, 160(6):509--516, 2004.

\bibitem[Wilson \& Hilferty(1931)]{wilsonhilferty1931}
E.~B. Wilson and M.~M. Hilferty. The distribution of chi-square. \emph{Proc.
Natl. Acad. Sci.}, 17(12):684--688, 1931.

\end{thebibliography}
}

\end{document}
